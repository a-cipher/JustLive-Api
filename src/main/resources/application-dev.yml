server:
  port: 8014

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/mixlive?serverTimezone=UTC&allowMultiQueries=true&characterEncoding=utf-8&useSSL=false&allowPublicKeyRetrieval=True
    username: root
    password: 123456
  redis:
    host: localhost
    port: 6379
    password:
  logging:
    file:
      path: ../logs

path:
  updateFilePath: ../update.txt
  bannerInfoFilePath: banner.txt

ali:
  mail:
    accessKeyId:
    accessKeySecret:

mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
